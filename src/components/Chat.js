import React, { Component } from 'react';
import ChatView from './ChatView';
import ChatForm from './ChatForm';

const Chat = () => {
  return (
    <div>
      <ChatView />
      <ChatForm />
    </div>
  );
};

export default Chat;
